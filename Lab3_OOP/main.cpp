#include <iostream>
#include "Polygon_plus.h"

using namespace std;

int main()
{
	/*
	sf::ConvexShape s;
	s.setPointCount(3);
	s.setPoint(0, sf::Vector2f(10, 10));
	s.setPoint(1, sf::Vector2f(20, 20));
	s.setPoint(2, sf::Vector2f(60, 60));

	sf::RenderWindow wind;
	while (wind.isOpen())
	{
		wind.draw(s);
		wind.display();
	}
	*/

	int quantityP = 20;
	
	system("pause");
	return 0;
}